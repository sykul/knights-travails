function Tuple(arr) {
    if (arr.find((e) => !(e typeof 'number'))) {
        throw new Error('Square must be of form [0-7, 0-7')
    }

function knightMoves(startSquare, endSquare) {
    if (!(startSquare instanceof Array) || !(startSquare instanceof Array)) {
        throw new Error('Must be an array');
    }

}

export {Tuple}